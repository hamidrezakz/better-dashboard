---
description: Dashboard segment — routes & nav SSOT; open dashboard.md for detail
globs: src/app/dashboard/**/*,src/app/api/dashboard/**/*
alwaysApply: false
---

# Dashboard segment

Reusable org/team dashboard slice — safe to fork or trim route trees + matching actions.

**URLs:** `dashboard-routes.ts` only. **Nav/tab/breadcrumb text:** `dashboard-nav-labels.ts` only.

Feature form/dialog copy stays local unless shared across the segment.

**Detail (Read):** [docs/agents/dashboard.md](docs/agents/dashboard.md).
