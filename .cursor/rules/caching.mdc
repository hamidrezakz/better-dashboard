---
description: Tagged cache — updateTag for same-user UI; open caching.md for detail
globs: src/app/action/**/*.ts,**/cache-tags.ts,src/app/**/lib/get-*.ts
alwaysApply: false
---

# Caching

In-repo Next docs — [nextjs.md](docs/agents/nextjs.md). Tags only via segment `cache-tags.ts`.

**Read:** `'use cache'` + `cacheTag` + `cacheLife`. **Write (actor must see fresh UI):** `updateTag` in the **same** action — not bare `revalidateTag(tag)`.

Session: never cache or revalidate — [implementation.md § Auth](docs/agents/implementation.md#auth--session-srclibauth-sessionts).

**Detail (Read):** [docs/agents/caching.md](docs/agents/caching.md).
